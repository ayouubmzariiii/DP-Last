// Barrel entry for design-sync: re-exports the app's reusable components as named
// exports so the converter bundles exactly these onto window.DPTravaux.
export { default as SealIcon } from '../src/components/SealIcon'
export { default as AuthForm } from '../src/components/AuthForm'
export { default as AddressAutocomplete } from '../src/components/AddressAutocomplete'
